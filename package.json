{
  "name": "kda-drishti-root",
  "version": "1.0.0",
  "main": "server/index.js",
  "scripts": {
    "install-all": "cd server && npm install",
    "start": "npm run install-all && node server/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
